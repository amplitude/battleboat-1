{
  "name": "battleboat-1",
  "version": "1.0.0",
  "description": "battleboat ==========",
  "main": "index.js",
  "scripts": {
    "build": "esbuild js/battleboat.js --bundle --outfile=dist/main.js",
    "start": "yarn build && yarn start:server",
    "start:server": "npx http-server -p 8001",
    "watch": "watch 'yarn build' ./js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/amplitude/battleboat-1.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/amplitude/battleboat-1/issues"
  },
  "homepage": "https://github.com/amplitude/battleboat-1#readme",
  "dependencies": {
    "@itly/plugin-amplitude": "^2.5.0",
    "@itly/plugin-schema-validator": "^2.4.0",
    "@itly/sdk": "^2.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.13.14",
    "http-server": "^14.0.0",
    "watch": "^1.0.2"
  }
}
